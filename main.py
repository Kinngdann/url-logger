from str_inputs import add_new_url, add_user
# from menu import Menu


def main():
    add_new_url()

    # menu = Menu()
    # menu.add_option(options=["Active urls", "Query results", "Modify url"])
    # menu.add_option("Active urls", ["opt1", "opt2", "opt3"])
    # menu.add_option("opt1", ["sub_opt1", "sub_opt2", "sub_opt3"])

    # menu.__str__()


if __name__ == '__main__':
    main()
